package net.acomputerdog.securitycheckup.main.gui.fxml.window;

import javafx.stage.Stage;

/**
 * A window is a stage that can stand alone and provide its own contents.
 */
public interface Window {
    Stage getStage();
}
